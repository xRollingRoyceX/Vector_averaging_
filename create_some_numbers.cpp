//[i like this equation for a stream of numbers]
//  i / (32 + 7) + 2 + (i * i) / 3 << endl;

#include"ref-nbr-vec"
void return_some_numbers()
{
     /* i like this equation for a stream of numbers]
     *           cout << i / (32 + 7) + 2 + (i * i) / 3 << endl;
     *
     * change a few numbers and stuff to get some "random" numbers 
     * to use as testing numbers for the function -
     * "comp_vec(const &vec, const vec&);" 
     *
     * remove comments here to use return_some_numbers -
     *  using this to just get, and place quasi-random values into 
     *  a calculator to help make sure the function is correct. 
     *  (linux calculator of course to easily check numbers );
    */
    
    for (int i = 0; i != 12; ++i) {
        //cout << i + 0.1 / (32 + 7) + 2.3 + (i * i) / 3.5 << ", ";
        //cout << i + 0.1 / (32 + 7) + 2.3 + (i * i) / 3.5 << " + ";
        //cout << i * i / (32.8 + 7.1) + 2 + (i * i) / 0.2 << ", ";
        //cout << i * i / (32.8 + 7.1) + 2 + (i * i) / 0.2 << " + ";
    }
}
